# libtmux-mcp deliberately starts without loading the user's tmux configuration.
set-option -g exit-empty off
set-option -g @libtmux_mcp_owner "$LIBTMUX_MCP_OWNER"
set-environment -gu LIBTMUX_MCP_OWNER
